version: '2.1'

volumes:

  pi-hole-data:
  dnsmasq-data:
  caddy-data:
  caddy-certs:
  ssh-data:

services:

  pi-hole:
    image: diginc/pi-hole-multiarch:debian_armhf
    ports:
      - '8080:80'
      - '192.168.86.12:53:53/tcp'
      - '192.168.86.12:53:53/udp'
    volumes:
      - 'pi-hole-data:/etc/pihole'
      - 'dnsmasq-data:/etc/dnsmasq.d'

  caddy:
    image: resin/caddy:v1.0.0
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - 'caddy-data:/var/lib/caddy'
      - 'caddy-certs:/root/.caddy'

  ssh:
    image: klutchell/resin-ssh
    ports:
      - '22:22'
    volumes:
      - 'ssh-data:/root/.ssh'
      - 'pi-hole-data:/pi-hole-data:ro'
      - 'dnsmasq-data:/dnsmasq-data:ro'
      - 'caddy-data:/caddy-data:ro'

