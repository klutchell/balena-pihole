version: '2.1'

volumes:

  pi-hole-data:
  dnsmasq-data:
  caddy-data:
  caddy-certs:
  ssh-data:

services:

  pi-hole:
    image: diginc/pi-hole-multiarch:debian_armhf
    ports:
      - '8080:80'
      - '192.168.86.12:53:53/tcp'
      - '192.168.86.12:53:53/udp'
    volumes:
      - 'pi-hole-data:/etc/pihole'
      - 'dnsmasq-data:/etc/dnsmasq.d'

  caddy:
    image: klutchell/caddy:rpi3-b239f5d
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - 'caddy-data:/usr/src/app'
      - 'caddy-certs:/root/.caddy'

  ssh:
    image: klutchell/ssh:rpi3-a0b03c9
    ports:
      - '22:22'
    volumes:
      - 'ssh-data:/root/.ssh'
      - 'pi-hole-data:/pi-hole-data:ro'
      - 'dnsmasq-data:/dnsmasq-data:ro'
      - 'caddy-data:/caddy-data:ro'

